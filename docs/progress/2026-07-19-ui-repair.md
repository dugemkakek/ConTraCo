# UI Repair + Multi-Venue Dashboard — Progress Log

> File: `docs/progress/2026-07-19-ui-repair.md`

## 2026-07-19 — Session

### ✅ Task 0: Progress log created
### ✅ Task 1: Multi-venue providers (OKX, Binance, Bybit, Kraken, Gate.io)
- 4 new providers: `binance_rest.py`, `bybit_rest.py`, `kraken_rest.py`, `okx_rest.py`
- Each has: `get_ohlcv()`, `get_all_spot_pairs()` (cached 1h, vol-sorted), `search_pairs()`
- Gate.io retrofitted with `get_all_spot_pairs()` + cache
- All 6 registered in `registry.py` — mock, gateio, binance, bybit, kraken, okx

### ✅ Task 2: Top-25 aggregate + TV prefix endpoints
- `GET /api/v1/market/top?limit=N` — top pairs by volume across ALL venues, deduped
- `GET /api/v1/market/tv-prefixes` — TV exchange prefix per venue
- TV prefix map: GATEIO, BINANCE, BYBIT, KRAKEN, OKX

### ✅ Plan updated
- `docs/plans/2026-07-19-ui-repair-and-dashboard-rebuild.md` revised:
  - "What's already built" section at top
  - Dashboard Task 4 now uses top-25 endpoint + TV prefixes
  - "What's already done" table in Out of Scope
  - Correct TV prefix table for all 5 exchanges

### ✅ Verification
- 81 backend tests passing
- All 6 providers import and instantiate cleanly
- Aggregate routes registered: `/api/v1/market/tv-prefixes`, `/api/v1/market/top`
- Pushed to `github.com/dugemkakek/ConTraCo`

### ⬜ Remaining (for cheaper model — all frontend)
| Task | File | What |
|------|------|------|
| 1 | `app/symbols/page.tsx` | Symbol browser with grid, filters, sort |
| 2 | Watchlist | DB model + API + `WatchlistWidget.tsx` |
| 3 | `app/dashboard/page.tsx` | **Rebuild** — top-25 strip, 3-panel layout, TV prefixes |
| 4 | `app/alerts/page.tsx`, `AlertBuilder.tsx` | Tabbed UI + create alert |
| 5 | `app/analytics/page.tsx` | Charts: equity, heatmap, gauge, drawdown |
| 6 | `app/journal/page.tsx` | Filters, sort, CSV export |
| 7 | `app/scan/page.tsx` | Per-symbol live progress |
| 8 | `app/backtest/page.tsx` | Form + equity curve + metrics |
| 9 | `app/settings/page.tsx` | Templates, risk limits, reset |
| 10 | Global | Toast, shortcuts, skeletons, depth |

